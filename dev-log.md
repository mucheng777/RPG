已经做好了人物基础状态机，起步停步，相机系统，动画系统关键帧设置那里不太好需要改

26/9/18
# 相机和输入功能冲突
## 发现问题
我发现我的相机系统里面用的cinemachine，主相机有Brain文件，第三人称相机也有CinemachineVirtualCamera，但是脚本里面又写的普通相机逻辑
好在我的cinemachine只管了跟随，但是滑动的太快又会出现抽搐的问题，大概率还是cinemachine自身和代码的转向有冲突

26/9/19
## 修改过程
**脚本**应该用**手写**的，用cinemachine根本没办法做简单的第三人称视角，cinemachine是用来做复杂相机切换的

所以现在第三人称相机完全由手写脚本控制，不过把inputsystem加进去了，变成单独的类了，**之前的inputsystem和cinemachine完全没办法配合做出第三人称简单相机**

**再也不瞎跟教程了，第三人称明明手写就很好了，非得和cinemachine放一起，根本塞不进去inputsystem**

26/9/20
# 斜面检测 接口
接下来是做一个斜面检测，拿ai生成了一个，给了五个脚本，直接惊呆我了，
用的接口，
接口ISlopeDataProvider.cs：声明"你得提供什么"（属性 + 事件）
逻辑层SlopeDetector.cs：实现接口——照着契约把每个属性填好、事件在合适的时机触发
消费层SlopeModelTilt.cs：只认接口，拿过来就读、就订阅，不关心底下谁在干活
还可以有一个配置层，直接可以让策划和美术上面改数据，还有个调试层，都是分开的

**以下为接口和逻辑层，消费层的关系**
┌─────────────────────────────────────────────────────────────┐
│                     接口（ISlopeDataProvider）                │
│  "我声明了：你能拿到 IsGrounded、IsOnSlope、GroundNormal..."    │
│  它是一份合同/菜单                                             │
└──────────────────────┬──────────────────────┬───────────────┘
                       │ 实现（签合同）         │ 依赖（看菜单点菜）
                       ▼                      ▼
┌──────────────────────────────┐   ┌─────────────────────────────┐
│     逻辑层（SlopeDetector）    │   │   消费方（PlayerController） │
│  "我来实现这些属性"            │   │  "我只要知道 IsOnSlope       │
│  内部怎么检测、防抖、滞回       │   │   是 true 还是 false"       │
│  全是我的事，谁都不用管"        │   │   我拿到值去调移动方向       │
│                               │   │                             │
│  每帧算完 → 填进 _committed    │──→│  GetComponent<接口>()       │
│  接口属性返回 _committed 的值  │   │  读接口属性 → 调整移动       │
└──────────────────────────────┘   └─────────────────────────────┘

**这个功能太好了，不敢动，回头再看看分析一下怎么做的**

